// This file used to register a route but that registration moved to
// `index.tsx`. Keep a small non-route placeholder component here so
// importers can use it if needed without causing route registration
// conflicts.
export default function TeacherPlaceholder() {
  return <div>Teacher placeholder</div>
}
