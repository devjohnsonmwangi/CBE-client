// Generated route tree (minimal)
// Files scanned: 2
export const routeFiles = [
  "routes/index.tsx",
  "routes/__root.tsx"
] as const

// Minimal routeTree stub so the app can import a routeTree during codegen
export const routeTree: any = {
  files: routeFiles,
}
